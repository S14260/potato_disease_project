# 孙凯

电话：17732218744 | 邮箱：sx14260@163.com | 微信：17732218744

---

## 教育背景

| 时间 | 学校 | 专业 |
|------|------|------|
| 2024.09 - 至今 | 天津工业大学 | 软件工程（硕士） |
| 2020.09 - 2024.07 | 南开大学滨海学院 | 计算机科学与技术（本科） |

---

## 专业技能

**Agent 与大模型开发（核心方向）**
- 熟悉 LangGraph 框架，掌握 Supervisor 多 Agent 编排模式，能够设计诊断、风险评估、治疗等多角色协作的 Agent 系统
- 熟悉 LangChain 工具链，掌握 `@tool` 装饰器封装、Function Calling 协议、ChatOpenAI 接口对接，能够从零构建 LLM 工具调用框架
- 熟悉 RAG（检索增强生成）技术栈，掌握 ChromaDB 向量数据库、HuggingFace Embedding 模型（bge-small-zh-v1.5）、语义检索与 metadata 过滤
- 熟悉 Prompt Engineering，能够针对不同 Agent 角色设计 system prompt，控制工具调用策略与输出格式

**Java 后端开发**
- 掌握 Spring Boot、Spring MVC、MyBatis 等主流开发框架，熟悉 RESTful API 设计规范

**遥感与空间数据处理**
- 熟悉 GeoTIFF 遥感数据处理流程，能够基于 Python（Rasterio、NumPy）完成栅格裁剪、区域提取、多时相影像加载与结果渲染

**数据库与缓存**
- 熟悉 MySQL 数据库设计与 SQL 优化，掌握 Redis 缓存机制，能够进行热点数据优化与接口性能优化

**GIS 地图开发**
- 熟悉 Leaflet 地图开发，具备空间数据可视化经验，如地块绘制、图层切换、地图交互等功能

**工程与集成能力**
- 熟悉 Java 后端、Python 空间计算、AI Agent 系统与前端地图应用的整合开发，具备从业务系统、GIS 可视化到 AI 辅助模块的完整工程落地能力
- 能够完成 Linux 环境部署、Flask 服务集成、Git 协作开发及多模块系统联调，具备较强的技术集成与问题排查能力

---

## 项目经历

### 马铃薯病害多 Agent 智能诊断系统

**时间：** 2026.03 - 2026.05

**技术栈：** Python / Flask / YOLOv8 / LangGraph / LangChain / ChromaDB RAG / Function Calling / OpenWeatherMap API

**项目描述：** 面向农业病害防治场景，构建集病害识别、环境风险评估、智能问诊与动态防治决策于一体的农业智能诊断平台，实现"病害检测—风险分析—知识检索—方案生成—动态干预"的业务闭环。

**核心工作：**

1. 设计并实现多 Agent 协作架构，基于 LangGraph Supervisor 模式将单体管道拆分为编排 Agent、诊断 Agent、风险评估 Agent、治疗 Agent 四个协作单元，通过 LLM Function Calling 实现自主任务分派与执行
2. 构建基于 ChromaDB 的农业知识向量检索系统，对病害信息、农药数据、SOP 流程进行向量嵌入（bge-small-zh-v1.5），实现带 metadata 过滤的语义检索，为大模型提供专业知识支撑
3. 基于 LangChain `@tool` 装饰器从零构建工具调用框架，封装 YOLO 检测、气象 API、风险引擎、知识库检索等 7 个工具，LLM 根据诊断上下文自主选择调用
4. 开发基于 SOP 的动态防治工作流，结合环境数据与农户反馈自动生成下一阶段防治方案，实现病害管理流程闭环

**项目亮点：**

1. **Multi-Agent 架构设计：** 采用 LangGraph Supervisor 模式，将诊断、风险评估、治疗三个领域解耦为独立 Agent，每个 Agent 拥有专属 system prompt 和工具集（2-3 个），通过编排 Agent 统一调度，避免单 Agent 工具集过大导致调用准确率下降
2. **RAG 知识增强：** 构建 ChromaDB 向量数据库，将病害、农药、SOP 数据向量化入库，支持自然语言语义检索（如"叶子长斑用什么药"匹配到早疫病信息），新增病害只需添加文档无需改代码
3. **Tool Calling 机制：** 基于 OpenAI 兼容协议实现 Function Calling，LLM 自主决定调用时机和参数；工具函数内置参数校验与结构化错误返回，Supervisor 设有 max_rounds 限制防循环调用，失败时自动回退到直接函数调用
4. **双模式架构：** 提供快速模式（直接函数调用 + LLM 报告生成，<5s 响应）和 Agent 模式（多 Agent 协作推理，完整 Tool Calling 链路），前端支持一键切换并可视化 Agent 协作流程

---

### 农田智能监测与巡田决策平台

**时间：** 2025.08 - 2026.05

**技术栈：** Spring Boot / MySQL / Redis / Leaflet / Python / Flask / HTML / JavaScript / Git

**项目描述：** 面向精准农业的农田监测平台，围绕地块管理、遥感影像处理与异常检测，实现"地块圈定—影像裁剪—异常识别—巡田调度—结果可视化"的完整业务闭环。

**核心工作：**

1. 负责后端系统设计与开发，实现用户管理、地块管理、巡田管理及相关数据接口服务
2. 基于 HTML + JavaScript + Layui 完成前端页面开发，实现地图交互与数据展示
3. 集成 Python 数据处理服务，通过 Flask 提供农田异常检测、巡田路径规划、遥感影像裁剪与 AI 圈地等接口

**项目亮点：**

1. **系统架构设计：** 采用"Java 业务层 + Python 计算层 + 可视化层"分层架构，实现空间计算与业务逻辑解耦
2. **数据处理能力：** 基于地块边界对 GeoTIFF 影像进行动态裁剪与按需加载，避免整幅影像传输，显著提升地图渲染性能与用户交互体验
3. **巡田任务调度：** 设计异步巡田任务处理机制，通过轮询获取异常检测结果；实现从异常检测、工单生成到巡田路径输出的闭环流程
4. **AI 能力集成：** 集成 YOLO 分割模型实现自动圈地；引入 LLM 对异常区域优先级与巡田建议进行辅助决策；基于 SSE 实现 AI 助手流式输出与多轮上下文对话，结合 Tool Calling 工具调用机制实现业务功能联动

---

### 餐饮订单管理系统

**时间：** 2024.09 - 2024.12

**技术栈：** Spring Boot / MyBatis / MySQL / Redis / JWT / Vue / WebSocket

**项目描述：** 面向餐饮场景的订单管理系统，实现从下单到履约的完整业务流程。

**核心工作：**

1. 负责后端系统开发，实现用户、商品、订单等核心模块接口设计与实现
2. 基于 JWT 实现用户认证与权限控制，区分用户端与管理端访问权限
3. 设计订单状态流转机制，实现订单从创建、支付、接单到完成的全流程管理

**项目亮点：**

1. **文件存储优化：** 集成阿里云 OSS，降低服务器存储压力，实现图片及文件的高效上传与下载
2. **性能优化能力：** 引入 Redis 缓存机制，减少数据库访问压力，提高接口响应效率
3. **任务调度系统：** 开发 SpringTask 定时扫描模块，自动处理超时订单；设计优先级队列，实现催单分级推送
4. **实时通信：** 基于 WebSocket 开发订单状态实时推送，并通过心跳检测机制保障连接稳定性

---

## 技能 / 优势及其他

- **语言：** CET-4（521 分），CET-6（466 分），具备良好的英文技术文档阅读能力
- **研究方向：** 智慧农业场景下的 Multi-Agent 智能决策系统、RAG 知识增强、农田异常检测与巡田路径规划
- **自我评价：** 本人性格乐观，热情开朗，能吃苦耐劳，注重规划，习惯总结。有较强的团队精神和抗压能力，有责任感。

---

## 面试问答准备

### Agent 架构相关

**Q: 为什么选择 Multi-Agent 而不是单个 Agent 加所有工具？**
A: 单 Agent 的 system prompt 会很长（需要描述所有工具和使用场景），工具集过大会导致 LLM 选择准确率下降。分 Agent 后每个 prompt 聚焦一个领域，工具集更小（2-3 个），LLM 更容易做出正确决策。另外，分 Agent 后可以独立测试和优化每个领域的 prompt。

**Q: Supervisor 怎么决定调用顺序？**
A: Supervisor 的 system prompt 描述了各 Agent 的能力和推荐顺序（诊断→风险→治疗）。LLM 根据 prompt 和用户请求自主决策。同时，prompt 中明确指定了默认顺序作为保底，确保流程可靠性。

**Q: 为什么不用 LangChain 的 AgentExecutor？**
A: AgentExecutor 是旧版模式，LangGraph 的 create_react_agent 是 2025+ 的推荐方案。LangGraph 返回的是状态图（StateGraph），支持流式输出、断点续传、人工介入等高级特性，且可以直接组合成 Supervisor 多 Agent 系统。

### RAG 相关

**Q: 为什么用 ChromaDB 而不是直接用字典？**
A: 字典只能精确匹配 key，不支持模糊查询。ChromaDB 是向量数据库，支持语义搜索——用户问"叶子长斑用什么药"能匹配到"早疫病"的相关信息。另外，新增病害只需添加文档，不修改代码，扩展性更好。

**Q: Embedding 模型选的什么？为什么？**
A: 使用 HuggingFace 的 `BAAI/bge-small-zh-v1.5`，中文优化的 sentence-transformer 模型。选择理由：(1) 中文支持好，对农业术语有较好的语义理解；(2) 本地运行，不需要外部 API；(3) 模型体积小（~100MB），推理速度快。

**Q: 两个病害的数据量很小，有必要用向量数据库吗？**
A: 从功能上看，当前数据量确实可以用字典。但用向量数据库的价值在于：(1) 架构可扩展，新增病害只需添加文档；(2) 支持语义搜索，用户体验更好；(3) 作为简历项目，展示了 RAG 技术栈的完整实现。

### Tool Calling 相关

**Q: Function Calling 和普通的 API 调用有什么区别？**
A: 普通 API 调用是程序员写死调用逻辑（if-else 决定调什么函数）。Function Calling 是 LLM 根据用户意图自主决定调用哪个函数、传什么参数。程序员只需定义工具列表和描述，LLM 负责决策。这是 Agent 系统的核心能力。

**Q: 如果 LLM 调用了错误的工具怎么办？**
A: 三层防御：(1) 每个 Agent 的 system prompt 明确限定了工具使用场景和前置条件；(2) 工具函数内部有参数校验，返回结构化错误信息；(3) Supervisor 有 max_rounds 限制防止循环调用，超时后回退到直接函数调用。

**Q: @tool 装饰器做了什么？**
A: 它读取函数的 docstring 作为工具描述（LLM 看到的 description），读取 type hints 生成 JSON Schema（定义参数类型和必填项），然后包装成 LangChain 的 BaseTool 对象。最终 LLM 看到的是标准的 OpenAI Function Calling 格式。

### 工程相关

**Q: 为什么选择 mimo-v2.5-pro 而不是 GPT-4？**
A: mimo-v2.5-pro 是通过小米 MiMo 平台提供的 OpenAI 兼容 API，支持 Function Calling 协议，且在国内访问稳定、成本更低。架构上使用 ChatOpenAI 接口，换模型只需改配置，不改代码。

**Q: 错误处理怎么做的？**
A: 三个层面：(1) 工具层面：每个工具函数内部 try-catch，返回错误信息而非抛异常；(2) Agent 层面：max_rounds 限制防止无限循环；(3) 系统层面：Supervisor 失败时回退到直接调用原始函数，确保用户总能得到结果。

**Q: 这个系统怎么扩展到更多病害？**
A: 三步：(1) 在 knowledge/*.py 中添加新病害的数据；(2) 重新运行 `python rag/ingest.py` 更新向量数据库；(3) 如果需要新的 YOLO 类别，重新训练模型。Agent 层面不需要任何修改，因为工具是通用的。
